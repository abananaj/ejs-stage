<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        body {
            font-family: sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }

        footer {
            border-top: 1px solid #ccc;
            padding-top: 10px;
            margin-top: 40px;
            color: #666;
            font-size: 0.9em;
        }
    </style>
</head>

<body>
    <header>
        <h1>
            <%= title %>
        </h1>
        <p>
            <%= subtitle %>
        </p>
    </header>

    <%- content %>

        <footer>
            <p>Built with EJS | <%= new Date().getFullYear() %>
            </p>
        </footer>
</body>

</html>